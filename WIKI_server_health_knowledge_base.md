# 服务器巡检运维知识库 WIKI 规范

## 一、知识库描述

### 可直接填写的简短描述

本知识库用于沉淀服务器巡检报告、资源指标趋势、指定进程与线程状态、Docker运行状态、应用端口、日志目录、异常日志、AI评估以及后续运维操作履历。知识以“报告—服务器—巡检快照—资源评估/日志事件—建议操作—执行结果—证据文件”的关系组织，为运维知识图谱 Agent 提供故障检索、历史追溯、同类异常关联、趋势比较和执行状态核验能力。知识库严格区分已观察事实、AI判断、待执行建议和已完成操作，不把建议写成已经实施的变更。

### 适用范围

- 输入来源：服务器巡检 TXT、服务器巡检 PDF、`*_运维执行履历.json`、DeepSeek评估结果和经确认的运维执行记录。
- 主要对象：服务器、巡检报告、巡检快照、资源指标、指定进程、Docker容器、应用端口、日志目录、日志事件、异常评估、建议操作、实际执行操作和证据文件。
- 主要用途：每日巡检归档、异常定位、服务器历史对比、资源趋势查询、重复异常关联、建议跟踪和已执行操作追溯。
- 不包含：未经证据确认的根因、凭空推测的部署关系、密钥或令牌、与目标巡检日期无关的历史日志，以及没有实际执行记录却被描述为已经完成的操作。

## 二、WIKI 内容生成要求

### 1. 数据来源优先级

生成 WIKI 时按以下顺序取值：

1. `*_运维执行履历.json`：作为知识图谱节点、关系、状态和证据引用的首选来源。
2. 服务器巡检 PDF：作为面向人员阅读的版式结果和汇总核对来源。
3. 原始巡检 TXT：用于核对采集原文、时间点、进程、端口、Docker和日志证据。
4. DeepSeek返回日志：只用于核对AI评估原文，不得覆盖TXT中的客观事实。

不同来源冲突时必须保留冲突说明，并标注各自来源，不得擅自选择其中一种作为事实。

### 2. WIKI 页面组织

每个巡检日期生成一篇日报页面，标题格式：

`服务器巡检报告 YYYY-MM-DD`

建议的页面层级：

```text
服务器运维知识库
├─ 巡检日报
│  └─ YYYY
│     └─ YYYY-MM-DD 服务器巡检报告
├─ 服务器档案
│  ├─ 飞马1
│  ├─ 飞马2
│  └─ Client
├─ 异常专题
│  ├─ 应用日志
│  ├─ Docker日志
│  ├─ 系统与进程日志
│  └─ 资源指标异常
└─ 运维执行履历
```

日报页负责记录当日事实；服务器档案页通过关系或反向链接汇总历史，不重复复制全部日志内容。

### 3. 日报必须包含的内容

1. 报告元数据：巡检日期、报告生成时间、PDF名称、TXT数量、服务器数量、AI评估状态和履历JSON名称。
2. 总体概要：各服务器总体状态、采集完整度、异常数量和最高重要程度。
3. 服务器概要：服务器备注、主机名、巡检时间点、脚本版本、权限状态、采集受限项和未纳入项。
4. 资源趋势：CPU、内存、磁盘、inode、iowait和磁盘设备util的时间序列、峰值、最新值及评估结果。
5. 服务状态：指定进程真实名称、线程数量、PID、Docker容器、应用端口、日志目录大小和业务目录名称。
6. 异常日志：应用日志优先，其后为Docker日志，最后为系统/进程日志；展示服务器、类别、重要程度、首次/末次发现时间、判断、建议、操作分析、评估来源和去重样例。
7. 运维履历：已完成的巡检采集与报告生成，以及待执行建议和后续实际执行结果。
8. 证据引用：每项重要判断应能追溯到履历节点、TXT文件、PDF或DeepSeek响应记录。

### 4. 事实与状态边界

- `completed`：只表示有证据证明已经发生，例如巡检采集完成、PDF生成完成或已有明确执行结果的操作。
- `proposed_not_executed`：AI给出的建议或操作分析，尚未执行。
- 后续若实际执行操作，应新增独立的 `OperationExecution` 记录，保存执行时间、执行人或执行主体、对象、命令或操作摘要、执行结果、验证结果和证据；不得直接修改原建议使其看起来已经执行。
- AI输出中的“判断”是评估结论，不等于已确认根因。没有部署配置、命令结果或运行日志佐证时，应使用“可能”“评估为”或“尚待核验”。
- 采集工具缺失或权限不足时，应写成“不可判定”或“采集受限”，不得写成服务不存在或端口未监听。
- 只提取目标 `date` 当天的日志。其他日期、无法确认日期的历史状态、Docker Started/Finished元数据不得进入当日异常或送入AI分析。

### 5. 日志去重与分类要求

- 相同服务器、相同语义的重复日志合并为一个事件；跨小时重复出现不重复计数。
- 保留首次发现时间、末次发现时间、代表性样例和来源章节。
- AI返回 `duplicate_of` 时，主事件保留完整内容，从事件仅建立重复关系，不在WIKI重复展开。
- 分类固定为：应用日志、Docker日志、系统/进程日志、资源指标异常。
- 日志展示顺序固定为：应用日志 → Docker日志 → 系统/进程日志；同一分类内按重要程度和发现时间排序。

### 6. 内容表达要求

- 使用简洁、可检索的中文标题，一项事实只表达一次。
- 服务器名称、进程名称、容器名称、端口、目录和指标名称必须使用报告中的实际值，不使用模糊占位词。
- 判断、建议、操作、来源必须分开，不能合并成一段难以结构化提取的文字。
- 数值必须带单位；百分比保留合理精度；时间使用 `YYYY-MM-DD HH:mm:ss` 或带时区的ISO 8601格式。
- 不输出API Key、Authorization、密码、Cookie、私钥或完整敏感请求头。
- 不为了充实内容补造根因、命令结果、负责人、处理时长或恢复结果。

## 三、WIKI 提取重点

### 1. 报告级信息

| 提取内容 | JSON位置 | 用途 |
|---|---|---|
| 报告ID | `root_report_id` | 日报的稳定主键 |
| 巡检日期 | `InspectionReport.properties.report_date` | 时间索引 |
| PDF路径 | `InspectionReport.properties.pdf_path` | 报告证据 |
| TXT数量 | `InspectionReport.properties.source_txt_count` | 数据覆盖度 |
| 服务器数量 | `InspectionReport.properties.server_count` | 报告范围 |
| AI状态 | `InspectionReport.properties.ai_assessment_status` | 区分AI或规则评估 |

### 2. 服务器与巡检快照

- 服务器稳定ID、备注和主机名。
- 每次巡检的准确时间及来源TXT。
- 总体状态、巡检模式、采集完整度、执行权限、脚本版本和配置文件。
- 已采集项、采集受限项和脚本未纳入项。
- 指定进程名称、线程数、PID和明细。
- Docker、应用端口、日志目录和业务目录状态。

### 3. 资源指标与趋势

- CPU、内存、磁盘、inode、iowait和util的每个时间点数值。
- 各指标最新值、峰值、变化方向和连续异常时段。
- AI/规则的重要程度、判断、建议、操作分析和来源。
- 指标异常必须关联服务器、报告日期和相关巡检快照，不能只保存一段无上下文的结论。

### 4. 日志事件

- 所属服务器、日志分类、事件标题、重要程度和是否需要处理。
- 首次发现时间、末次发现时间、去重样例、来源章节和去重主事件ID。
- AI判断、建议、操作分析及评估来源。
- 只保留目标日期日志；样例中若含其他日期，应作为数据质量问题标记并从当日事件中排除。

### 5. 运维执行履历

重点提取并区分：

- 巡检采集：何时采集、采集哪台服务器、使用哪个TXT证据、结果是否完成。
- 报告生成：何时生成、生成哪个PDF和履历JSON、结果是否完成。
- 待执行建议：建议内容、操作分析、关联异常或指标、来源、状态 `proposed_not_executed`。
- 实际执行记录：执行时间、执行主体、目标服务器、执行内容、执行结果、验证结果、回滚信息和证据。
- 建议与执行记录必须通过关系关联，但二者不能合并为同一节点。

### 6. 知识图谱关系重点

优先提取下列关系：

```text
报告 COVERS_SERVER 服务器
报告 CONTAINS_SNAPSHOT 巡检快照
报告 USES_EVIDENCE TXT证据
巡检快照 OBSERVED_SERVER 服务器
巡检快照 SUPPORTED_BY TXT证据
报告 CONTAINS_ASSESSMENT 资源评估
资源评估 ASSESSES_SERVER 服务器
报告 CONTAINS_LOG_EVENT 日志事件
日志事件 OBSERVED_ON 服务器
异常或指标 PROPOSES_ACTION 待执行建议
报告生成操作 GENERATED 报告
```

后续实际执行运维操作时，建议补充：

```text
实际操作 EXECUTES 建议
实际操作 TARGETS 服务器
实际操作 RESOLVES 或 MITIGATES 异常事件
实际操作 VERIFIED_BY 验证证据
实际操作 ROLLED_BACK_BY 回滚操作
```

## 四、WIKI 日报页面模板

```markdown
# 服务器巡检报告 YYYY-MM-DD

## 报告信息
- 报告ID：
- 生成时间：
- PDF：
- 运维履历JSON：
- 输入TXT：
- AI评估状态：

## 总体状态
| 服务器 | 主机名 | 最新巡检时间 | 总体状态 | 采集状态 | 最高重要度 |
|---|---|---|---|---|---|

## 服务器资源与服务概要
### 服务器名称
- 资源趋势：
- 指定进程与线程：
- Docker与应用端口：
- 日志目录与业务目录：
- 采集受限/未纳入：

## 当日异常
### 应用日志
### Docker日志
### 系统与进程日志
### 资源指标异常

每条异常均使用：服务器、重要程度、发现时间、判断、建议、操作分析、来源、证据。

## 运维执行履历
| 时间 | 服务器/对象 | 事件类型 | 状态 | 内容 | 结果/证据 |
|---|---|---|---|---|---|

## 数据质量与待核验项
- 采集受限：
- 来源冲突：
- 尚待核验：
```

## 五、质量验收要求

- 每台服务器至少关联一个巡检快照和一个TXT证据，除非明确标记缺失原因。
- 所有关系的 `source` 和 `target` 必须指向存在的节点ID。
- 每条异常和资源评估必须关联具体服务器。
- 每条建议必须有状态；没有实际执行证据时只能是 `proposed_not_executed`。
- WIKI中的异常数量应与履历JSON去重后的主事件数量一致。
- WIKI不得出现目标日期之外的日志作为当日异常。
- WIKI不得泄露密钥、令牌或认证信息。
- 无法确认的信息必须明确写成“不可判定”“采集受限”或“尚待核验”。
